# Per-mount disk view

Add a disk view that lists each mounted filesystem with its mount point, total, used, and available space, plus usage percentage. Keep collection platform-specific and the shared display and output portable across macOS and Linux.
